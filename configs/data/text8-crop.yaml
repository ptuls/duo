# TODO: When using this dataset, set model.length = 256 to match D3PM setup
train: text8-crop
valid: text8
modality: text
tokenizer_name_or_path: text8
cache_dir: ${oc.env:DUO_DATA_DIR,${oc.env:HOME}/.cache/duo_data}
wrap: True
streaming: False
insert_train_eos: True
insert_valid_eos: True